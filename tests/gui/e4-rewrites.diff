diff --git a/Jellyfin.Plugin.SubSync/Web/subsyncMain.html b/Jellyfin.Plugin.SubSync/Web/subsyncMain.html
index 14bfde7..96e7c5c 100644
--- a/Jellyfin.Plugin.SubSync/Web/subsyncMain.html
+++ b/Jellyfin.Plugin.SubSync/Web/subsyncMain.html
@@ -206,7 +206,7 @@
 
                         <section id="panel-settings" class="ss-hidden">
                             <h2 class="sectionTitle" style="margin:0;">Settings</h2>
-                            <p class="sectionDescription ss-muted" style="margin-top:4px;">Sync engine options. ffmpeg and the bundled ffsubsync are detected automatically — only change these if you know what you are doing.</p>
+                            <p class="sectionDescription ss-muted" style="margin-top:4px;">Sync engine options. ffmpeg and the bundled ffsubsync are detected automatically; the fields here override them.</p>
 
                             <div id="ss-status" style="padding:10px 12px;border-radius:6px;background:rgba(255,255,255,.06);color:var(--text-color,#ddd);white-space:pre-wrap;word-break:break-word;font-size:.85rem;line-height:1.55;margin:10px 0 4px;">Checking status...</div>
                             <div id="ss-identity" class="ss-muted" style="font-size:.74rem;opacity:.6;margin:0 0 4px;word-break:break-all;"></div>
@@ -227,7 +227,10 @@
                                 </div>
                                 <div class="ss-field">
                                     <input is="emby-input" type="number" id="ss-maxoffset" label="Max offset seconds" min="1" max="600" />
-                                    <div class="fieldDescription">How far from the video's audio the alignment may look for a subtitle's position. 180 by default: a subtitle can genuinely be minutes out, and an answer outside this window cannot be found. Too large invites matching the wrong place. A result that lands on the window is retried with twice it, and only written when the film's audio agrees.</div>
+                                    <div class="fieldDescription">How far from the video's audio the alignment may look.<br>
+                                        <strong>180</strong> by default: a subtitle can genuinely be minutes out, and an answer outside the window cannot be found at all.<br>
+                                        Too large invites matching the wrong place.<br>
+                                        A result that lands exactly on the window is retried with twice it, and written only when the film's audio agrees.</div>
                                 </div>
                                 <div class="ss-field">
                                     <input is="emby-input" type="number" id="ss-maxrefoffset" label="Max shift from a subtitle reference" min="1" max="600" />
@@ -239,7 +242,9 @@
                                 </div>
                                 <div class="ss-field">
                                     <input is="emby-input" type="number" id="ss-splitpenalty" label="Split penalty (piecewise alignment)" min="0" max="50" step="1" />
-                                    <div class="fieldDescription">Lets the offset change across the timeline, for a file that is not one continuous cut (commercial breaks, inserted or removed scenes, discs joined into one file). 0 = a single offset for the whole file, which is what the plugin has always done. Values around 4-20 are typical; lower splits more eagerly. Default: 0.</div>
+                                    <div class="fieldDescription">Lets the offset change across the timeline, for a file that is not one continuous cut (commercial breaks, inserted or removed scenes, discs joined into one file).<br>
+                                        <strong>0</strong> (default) = one offset for the whole file.<br>
+                                        4-20 is typical; a lower value splits more readily.</div>
                                 </div>
                                 <div class="ss-field">
                                     <select is="emby-select" id="ss-encoding" label="Output encoding">
@@ -249,7 +254,7 @@
                                         <option value="latin-1">Latin-1 (ISO-8859-1) — for older western-European players</option>
                                         <option value="ascii">ASCII — drops letters outside English; only for a track that has none</option>
                                     </select>
-                                    <div class="fieldDescription">The encoding the synced subtitle file is written in. Only the values the engine accepts are offered, so a typo cannot be saved and then silently written as UTF-8.</div>
+                                    <div class="fieldDescription">The encoding the synced subtitle file is written in. Only values the engine accepts are offered.</div>
                                 </div>
 
                                 <h3 class="ss-subhead">Multi-subtitle sync mode</h3>
@@ -275,7 +280,10 @@
                                 </div>
                                 <div class="ss-field">
                                     <input is="emby-input" type="number" id="ss-workers-input" label="Parallel workers" min="1" max="64" />
-                                    <div class="fieldDescription">How many subtitles run at once in Parallel mode. Default: 4. Each wave covers different media files — several tracks of the same file never run at once. Reading subtitles out of the video runs in its own lanes, half this number and at most 3, because a lane reads a file while the workers align files already read. A higher value applies as soon as you save; a lower one as the extractions already running finish.</div>
+                                    <div class="fieldDescription">How many subtitles run at once in Parallel mode. Default: <strong>4</strong>.<br>
+                                        Each wave covers different media files — several tracks of the same file never run at once.<br>
+                                        Extraction has its own lanes (half this number, at most 3), so a file is read while other files are aligned.<br>
+                                        A higher value applies as soon as you save; a lower one as the running extractions finish.</div>
                                 </div>
                                 <div class="ss-field">
                                     <label class="selectLabel">Cached data</label>
@@ -298,10 +306,9 @@
                                 <div class="ss-field">
                                     <label class="selectLabel" for="ss-langadd">Only sync subtitles in these languages</label>
                                     <div class="fieldDescription">
-                                        Leave empty to sync every language. Add one entry per language — anything else is never
-                                        listed and never synced. Image-based tracks (Blu-ray PGS, DVD VobSub, DVB, XSUB) are
-                                        excluded from the lists entirely, since they cannot be aligned. While a filter is set,
-                                        text tracks with no language at all are skipped too, because they cannot be matched.
+                                        Leave empty to sync every language. One entry per language; anything else is never listed and never synced.<br>
+                                        Image-based tracks (Blu-ray PGS, DVD VobSub, DVB, XSUB) are left out of the lists entirely — they cannot be aligned.<br>
+                                        With a filter set, text tracks that carry no language are skipped too: they cannot be matched.
                                     </div>
                                     <div class="ss-langrow">
                                         <div class="ss-langfield">
@@ -320,7 +327,9 @@
                                             <input is="emby-checkbox" type="checkbox" id="ss-fixfps" />
                                             <span>Correct framerate mismatch</span>
                                         </label>
-                                        <div class="fieldDescription checkboxFieldDescription">On by default: a subtitle timed for a different framerate (PAL 25 on a 23.976 fps file) is stretched onto the video's timeline, whether the alignment uses the audio or another subtitle. Turn it off when a subtitle from a different cut gets stretched instead — that case looks the same in the numbers, and the stretch then leaves the end of the film minutes out of step. Nothing is stretched silently either way: a result that is not a real framerate pair is refused with the numbers.</div>
+                                        <div class="fieldDescription checkboxFieldDescription">On by default: a subtitle timed for a different framerate (PAL 25 on a 23.976 fps file) is stretched onto the video's timeline, whether the alignment used the audio or another subtitle.<br>
+                                        Turn it off when a subtitle from a different cut gets stretched instead — the stretch then leaves the end of the film minutes out of step.<br>
+                                        Nothing is stretched silently: a result that is not a real framerate pair is refused with the numbers.</div>
                                     </div>
                                 </div>
                                 <div class="ss-field">
@@ -329,7 +338,7 @@
                                             <input is="emby-checkbox" type="checkbox" id="ss-gss" />
                                             <span>Use golden-section search for framerate ratio</span>
                                         </label>
-                                        <div class="fieldDescription checkboxFieldDescription">Searches harder for the exact stretch factor. Slower, and it only does anything together with <strong>Correct framerate mismatch</strong> above: while that is off the switch is unavailable and the engine is not given the flag.</div>
+                                        <div class="fieldDescription checkboxFieldDescription">Searches harder for the exact stretch factor. Slower, and only useful together with <strong>Correct framerate mismatch</strong> above.</div>
                                     </div>
                                 </div>
                                 <div class="ss-field">
@@ -337,7 +346,9 @@
                                         <option value="copy">Keep original — save a synced copy</option>
                                         <option value="replace">Replace the original subtitle</option>
                                     </select>
-                                    <div class="fieldDescription">Recommended (default): each sync writes a new file named "Title.SYNCED.srt" next to the original, so your subtitles are never touched. Replace overwrites the subtitle in place. Video files are never modified — embedded tracks are extracted and saved as a new external subtitle file instead of being remuxed back in.</div>
+                                    <div class="fieldDescription">Recommended (default): each sync writes a new file — <strong>Title.SYNCED.srt</strong> — next to the original, so your subtitles are never touched.<br>
+                                        Replace overwrites the subtitle in place.<br>
+                                        Video files are never modified: an embedded track is extracted to a new external subtitle file, never remuxed back in.</div>
                                 </div>
                                 <h3 class="ss-subhead">Paths (Advanced)</h3>
                                 <div class="ss-field">
